---
phase: 02-box-lifecycle
plan: 01
type: execute
wave: 1
depends_on:
  - 01-database-authentication
files_modified:
  - TODO.md
  - MothersonBoxManagement/Entities/Box.cs
  - MothersonBoxManagement/Data/Dtos/CreateBoxDto.cs
  - MothersonBoxManagement/Data/Dtos/BoxDetailsDto.cs
  - MothersonBoxManagement/Data/ApplicationDbContext.cs
  - MothersonBoxManagement/ViewModels/CreateBoxViewModel.cs
  - MothersonBoxManagement/Views/Box/Create.cshtml
  - MothersonBoxManagement/Migrations/<timestamp>_UseIntegerBoxDimensions.cs
  - MothersonBoxManagement/Migrations/ApplicationDbContextModelSnapshot.cs
autonomous: true
requirements:
  - BOX-01
  - BOX-02
  - BOX-03
requirements_note: "BOX-06 is intentionally left to Phase 3 scan auto-completion and must not expand in Phase 2."
must_haves:
  truths:
    - "TSK-007 through TSK-010 are marked En cours before code edits, per AGENT.md workflow."
    - "BOX-01: authenticated users can create Carton, Bois, or Plastique boxes."
    - "BOX-02 and D-10: Height, Width, and Depth are positive integer centimeters across ViewModel, DTO, entity, EF schema, and view inputs."
    - "BOX-03: ExpectedQuantity is an integer strictly greater than zero."
  artifacts:
    - "TODO.md records Phase 2 execution status before implementation."
    - "UseIntegerBoxDimensions EF Core migration exists and model snapshot reflects integer dimensions."
  key_links:
    - "CreateBoxViewModel validation -> BoxController mapping -> CreateBoxDto -> Box entity -> EF migration"
---

<objective>
Prepare Phase 2 workflow status and implement integer box creation inputs for BOX-01, BOX-02, and BOX-03 per D-08, D-09, D-10, and D-11.

Purpose: satisfy the project workflow before code edits and align persisted box dimensions with the locked integer-centimeter decision.
Output: TODO status update, integer dimension model changes, migration, and focused build/migration verification.
</objective>

<execution_context>
@C:/Users/mehdi/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/mehdi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-box-lifecycle/02-CONTEXT.md
@.planning/phases/02-box-lifecycle/02-RESEARCH.md
@.planning/phases/02-box-lifecycle/02-VALIDATION.md
@AGENT.md
@TODO.md
@MothersonBoxManagement/Entities/Box.cs
@MothersonBoxManagement/Data/ApplicationDbContext.cs
@MothersonBoxManagement/ViewModels/CreateBoxViewModel.cs
@MothersonBoxManagement/Views/Box/Create.cshtml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark Phase 2 tasks in progress before code edits</name>
  <files>TODO.md</files>
  <action>Update TSK-007, TSK-008, TSK-009, and TSK-010 from Backlog to En cours before modifying production code, per AGENT.md section 21. Update the reference column to Phase 2 / Plan 02-01 through 02-04 where appropriate without changing Phase 3 responsibilities.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path TODO.md -Pattern 'TSK-00[7-9]|TSK-010' | Select-String -Pattern 'En cours'"</automated>
  </verify>
  <done>TSK-007 through TSK-010 are visibly En cours before Phase 2 implementation edits begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert box dimensions to positive integer centimeters</name>
  <files>MothersonBoxManagement/Entities/Box.cs; MothersonBoxManagement/Data/Dtos/CreateBoxDto.cs; MothersonBoxManagement/Data/Dtos/BoxDetailsDto.cs; MothersonBoxManagement/ViewModels/CreateBoxViewModel.cs; MothersonBoxManagement/Views/Box/Create.cshtml</files>
  <behavior>
    - BOX-01: valid Carton, Bois, and Plastique selections remain accepted for authenticated users.
    - BOX-02: Height, Width, and Depth accept whole numbers greater than zero and reject zero, negative, and decimal values.
    - BOX-03: ExpectedQuantity remains an integer strictly greater than zero.
  </behavior>
  <action>Implement BOX-01, BOX-02, and BOX-03 with dimensions as int values in entity, DTOs, and CreateBoxViewModel. Replace decimal validation with integer range validation. Update Razor number inputs to min 1 and step 1. Keep controllers thin; do not expose EF entities to views.</action>
  <verify>
    <automated>dotnet build MothersonBoxManagement/MothersonBoxManagement.csproj</automated>
  </verify>
  <done>Active Phase 2 box creation types and dimensions compile as integer centimeters with server-side validation and matching Razor inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Generate integer dimension migration</name>
  <files>MothersonBoxManagement/Data/ApplicationDbContext.cs; MothersonBoxManagement/Migrations/&lt;timestamp&gt;_UseIntegerBoxDimensions.cs; MothersonBoxManagement/Migrations/ApplicationDbContextModelSnapshot.cs</files>
  <action>Generate an EF Core migration named UseIntegerBoxDimensions for the Height, Width, and Depth type change. The migration must alter SQL Server storage from floating-point to integer-compatible columns and update ApplicationDbContextModelSnapshot. Do not modify or delete prior migrations.</action>
  <verify>
    <automated>dotnet ef migrations list --project MothersonBoxManagement --startup-project MothersonBoxManagement</automated>
  </verify>
  <done>UseIntegerBoxDimensions appears in the migration list and the snapshot reflects integer box dimensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> MVC model binding | Untrusted form values enter CreateBoxViewModel. |
| MVC/service -> SQL Server | Validated dimensions and expected quantity are persisted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-S | Spoofing | Box creation route | medium | mitigate | Preserve existing authenticated access and role claim use from Phase 1; all roles may create per D-08. |
| T-02-01-T | Tampering | CreateBoxViewModel | medium | mitigate | Enforce integer positive range server-side, not only HTML attributes. |
| T-02-01-D | Denial of Service | Oversized numeric input | low | mitigate | Use int model binding and range validation to reject invalid values before persistence. |
| T-02-01-SC | Tampering | NuGet installs | low | accept | No package installs in this plan; existing EF tools are already referenced. |
</threat_model>

<verification>
Focused checks: `dotnet build MothersonBoxManagement/MothersonBoxManagement.csproj` and `dotnet ef migrations list --project MothersonBoxManagement --startup-project MothersonBoxManagement`.
</verification>

<success_criteria>
BOX-01, BOX-02, and BOX-03 have a valid creation contract, integer dimension persistence, and TODO status compliance before implementation.
</success_criteria>

<output>
Create `.planning/phases/02-box-lifecycle/02-01-SUMMARY.md` when done.
</output>
