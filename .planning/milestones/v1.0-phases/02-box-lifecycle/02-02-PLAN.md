---
phase: 02-box-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - MothersonBoxManagement/Services/IBoxService.cs
  - MothersonBoxManagement/Services/BoxService.cs
  - MothersonBoxManagement/Data/Dtos/BoxListItemDto.cs
  - MothersonBoxManagement/Data/Dtos/BoxDetailsDto.cs
  - MothersonBoxManagement/Controllers/BoxController.cs
  - MothersonBoxManagement/Views/Box/Details.cshtml
  - MothersonBoxManagement/Views/Box/Prepare.cshtml
  - MothersonBoxManagement.Tests/BoxControllerTests.cs
  - MothersonBoxManagement.Tests/ScanControllerTests.cs
autonomous: true
requirements:
  - BOX-04
  - BOX-05
  - BOX-07
requirements_note: "BOX-06 is intentionally left to Phase 3 scan auto-completion and must not expand in Phase 2."
must_haves:
  truths:
    - "BOX-04 and D-01/D-02/D-03: BoxNumber and BarcodeValue are identical and match BOX-YYYYMMDD-XXXXXX with a 6-character uppercase hex suffix; no BX prefix remains."
    - "BOX-05: box creation stores creator user id/matricule and creation timestamp."
    - "BOX-07: details display box properties, associated packages, progress statistics, and a Phase 4-backed audit/history section when logs are absent."
    - "D-14: open boxes have a distinct /Box/Prepare/{id} route."
  artifacts:
    - "BoxService creates collision-checked BOX identifiers."
    - "BoxController exposes GET /Box/Prepare/{id}."
    - "Views/Box/Prepare.cshtml exists."
  key_links:
    - "BoxService identity generation -> SQL unique indexes -> details/prepare display"
    - "Home lookup in later plan -> Box/Prepare route from this plan"
---

<objective>
Implement Phase 2 box identity, creator metadata, read-only details, and the open-box preparation route for BOX-04, BOX-05, and BOX-07.

Purpose: remove the stale BX behavior and create the route target required by homepage lookup without expanding Phase 3 scanner internals.
Output: service/controller/view/test changes for box identity and preparation routing.
</objective>

<execution_context>
@C:/Users/mehdi/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/mehdi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-box-lifecycle/02-CONTEXT.md
@.planning/phases/02-box-lifecycle/02-RESEARCH.md
@.planning/phases/02-box-lifecycle/02-VALIDATION.md
@MothersonBoxManagement/Services/BoxService.cs
@MothersonBoxManagement/Controllers/BoxController.cs
@MothersonBoxManagement/Views/Box/Details.cshtml
@MothersonBoxManagement.Tests/BoxControllerTests.cs
@MothersonBoxManagement.Tests/ScanControllerTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate box barcode equal to box number</name>
  <files>MothersonBoxManagement/Services/IBoxService.cs; MothersonBoxManagement/Services/BoxService.cs; MothersonBoxManagement/Data/Dtos/BoxDetailsDto.cs; MothersonBoxManagement.Tests/BoxControllerTests.cs; MothersonBoxManagement.Tests/ScanControllerTests.cs</files>
  <behavior>
    - BOX-04: creating a box stores BoxNumber and BarcodeValue as the same value.
    - BOX-04: generated identifiers match BOX-YYYYMMDD-XXXXXX with a six-character uppercase hexadecimal suffix.
    - BOX-05: creator id/matricule and CreatedAt are persisted and returned in details data.
  </behavior>
  <action>Implement BOX-04 and BOX-05 in BoxService.CreateBoxAsync. Generate BOX-{yyyyMMdd}-{six uppercase hex chars}; assign BarcodeValue = BoxNumber; add bounded collision retry against BoxNumber and BarcodeValue uniqueness. Add CancellationToken parameters to touched IBoxService methods and propagate EF async calls. Update stale scan guard tests or logic so box code checks use the BOX prefix per D-03 without implementing BOX-06 auto-completion.</action>
  <verify>
    <automated>dotnet test MothersonBoxManagement.Tests/MothersonBoxManagement.Tests.csproj --filter "FullyQualifiedName~BoxControllerTests|FullyQualifiedName~ScanControllerTests"</automated>
  </verify>
  <done>Created boxes use one BOX identifier for both number and barcode, creator metadata is preserved, and tests no longer expect BX behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add open-box preparation route and read-only details</name>
  <files>MothersonBoxManagement/Controllers/BoxController.cs; MothersonBoxManagement/Views/Box/Details.cshtml; MothersonBoxManagement/Views/Box/Prepare.cshtml; MothersonBoxManagement.Tests/BoxControllerTests.cs</files>
  <behavior>
    - D-14: GET /Box/Prepare/{id} renders preparation only for Open boxes.
    - HOME-03 support: non-open boxes resolve to read-only /Box/Details/{id}.
    - BOX-07: details show properties, packages, progress, and an audit/history placeholder tied to Phase 4 when logs are absent.
  </behavior>
  <action>Create GET /Box/Prepare/{id} in BoxController. Load the same BoxDetailsDto as details, return NotFound when missing, redirect non-open boxes to Details, and render Views/Box/Prepare.cshtml for open boxes. Move the open-box scan form out of Details.cshtml or share a partial so Details remains read-only for non-open statuses. Keep Phase 3 scanner listener, simulator, duplicate-scan transaction handling, and BOX-06 auto-completion out of scope.</action>
  <verify>
    <automated>dotnet test MothersonBoxManagement.Tests/MothersonBoxManagement.Tests.csproj --filter "FullyQualifiedName~BoxControllerTests"</automated>
  </verify>
  <done>/Box/Prepare/{id} exists for open boxes, non-open boxes redirect to read-only details, and details still satisfies BOX-07 display requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service -> SQL Server | Generated identifiers cross into unique persisted columns. |
| Browser -> BoxController route | Untrusted route id controls details/prepare lookup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-T | Tampering | Box barcode generation | high | mitigate | Server generates BoxNumber and BarcodeValue; user input never supplies either value. |
| T-02-02-R | Repudiation | Box creation metadata | medium | mitigate | Persist CreatedByUserId and CreatedAt and expose them in details DTO. |
| T-02-02-I | Information Disclosure | Details/Prepare route id | low | mitigate | Preserve authorization and NotFound behavior; do not expose database internals. |
| T-02-02-D | Denial of Service | Identifier collision loop | low | mitigate | Use a small bounded retry and fail with controlled service error after exhaustion. |
| T-02-02-SC | Tampering | NuGet installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
Focused command: `dotnet test MothersonBoxManagement.Tests/MothersonBoxManagement.Tests.csproj --filter "FullyQualifiedName~BoxControllerTests|FullyQualifiedName~ScanControllerTests"`.
</verification>

<success_criteria>
BOX-04, BOX-05, BOX-07, and D-14 route behavior are implemented without pulling Phase 3 scanner scope forward.
</success_criteria>

<output>
Create `.planning/phases/02-box-lifecycle/02-02-SUMMARY.md` when done.
</output>
