---
phase: 02-box-lifecycle
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - MothersonBoxManagement/ViewModels/HomeViewModel.cs
  - MothersonBoxManagement/Data/Dtos/BoxListItemDto.cs
  - MothersonBoxManagement/Services/BoxService.cs
  - MothersonBoxManagement/Controllers/HomeController.cs
  - MothersonBoxManagement/Views/Home/Index.cshtml
  - MothersonBoxManagement.Tests/BoxControllerTests.cs
autonomous: true
requirements:
  - BOX-08
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
requirements_note: "BOX-06 is intentionally left to Phase 3 scan auto-completion and must not expand in Phase 2."
must_haves:
  truths:
    - "BOX-08 and D-04/D-05/D-06/D-07: homepage dashboard shows all open boxes in a Bootstrap table, newest first, with progress, last update timestamp, and last user."
    - "HOME-01 and D-12: homepage box barcode input autofocuses."
    - "HOME-02 and D-14: known open box barcode redirects to /Box/Prepare/{id}."
    - "HOME-03 and D-14: known non-open box barcode redirects to /Box/Details/{id}."
    - "HOME-04 and D-15: package-looking barcode input shows an explicit warning without DB redirect."
  artifacts:
    - "Typed HomeViewModel in the ViewModels namespace."
    - "HomeController GET/POST use HomeViewModel and CancellationToken."
    - "Home dashboard table replaces cards."
  key_links:
    - "HomeController POST -> BoxService lookup -> Box status -> Prepare/Details redirect"
    - "BoxService.GetOpenBoxesAsync -> HomeViewModel.OpenBoxes -> Bootstrap table"
---

<objective>
Implement the Phase 2 home dashboard and barcode lookup flow for BOX-08 and HOME-01 through HOME-04.

Purpose: give operators an immediately scannable homepage that shows open boxes and routes box barcodes to the correct target.
Output: typed home ViewModel, dashboard table, status-aware lookup, and focused home regression tests.
</objective>

<execution_context>
@C:/Users/mehdi/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/mehdi/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-box-lifecycle/02-CONTEXT.md
@.planning/phases/02-box-lifecycle/02-RESEARCH.md
@.planning/phases/02-box-lifecycle/02-VALIDATION.md
@MothersonBoxManagement/Controllers/HomeController.cs
@MothersonBoxManagement/ViewModels/HomeViewModel.cs
@MothersonBoxManagement/Views/Home/Index.cshtml
@MothersonBoxManagement/Services/BoxService.cs
@MothersonBoxManagement.Tests/BoxControllerTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused home dashboard and lookup tests</name>
  <files>MothersonBoxManagement.Tests/BoxControllerTests.cs</files>
  <behavior>
    - BOX-08: homepage includes open boxes, excludes non-open boxes, and orders open boxes newest first.
    - HOME-01: homepage scan input has autofocus.
    - HOME-02: open box lookup redirects to /Box/Prepare/{id}.
    - HOME-03: non-open box lookup redirects to /Box/Details/{id}.
    - HOME-04: package barcode input shows the explicit package warning and stays on homepage.
  </behavior>
  <action>Add focused tests before or alongside implementation so this plan directly verifies HOME-01 through HOME-04 and BOX-08. Use existing login/test factory patterns. Keep package scan internals and BOX-06 outside assertions.</action>
  <verify>
    <automated>dotnet test MothersonBoxManagement.Tests/MothersonBoxManagement.Tests.csproj --filter "FullyQualifiedName~BoxControllerTests"</automated>
  </verify>
  <done>HomeController and homepage behavior have focused regression coverage for all requirements in this plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed home dashboard and status-aware barcode lookup</name>
  <files>MothersonBoxManagement/ViewModels/HomeViewModel.cs; MothersonBoxManagement/Data/Dtos/BoxListItemDto.cs; MothersonBoxManagement/Services/BoxService.cs; MothersonBoxManagement/Controllers/HomeController.cs; MothersonBoxManagement/Views/Home/Index.cshtml</files>
  <behavior>
    - BOX-08: OpenBoxes has box number, type, progress counts, last update timestamp, and last user.
    - HOME-02: open boxes redirect to the Prepare action created in 02-02.
    - HOME-04: package-looking values are rejected before database lookup.
  </behavior>
  <action>Move HomeViewModel to the MothersonBoxManagement.ViewModels namespace if needed. Replace ViewBag plumbing with typed model properties for matricule, role, barcode, error/warning, and IReadOnlyList&lt;BoxListItemDto&gt; OpenBoxes. Extend BoxListItemDto and GetOpenBoxesAsync with LastUpdatedAt = UpdatedAt ?? CreatedAt and LastUserMatricule = LastModifiedBy.Matricule ?? CreatedBy.Matricule; preserve newest-first sorting by CreatedAt descending and no pagination. Update HomeController GET/POST to accept CancellationToken, trim input, reject non-BOX package-looking input with the D-15 warning, show D-13 not-found for unknown BOX values, redirect Open to /Box/Prepare/{id}, and redirect all non-open statuses to /Box/Details/{id}. Update Index.cshtml to use a Bootstrap table, autofocus input, progress bar, and JavaScript interval refresh per D-05.</action>
  <verify>
    <automated>dotnet test MothersonBoxManagement.Tests/MothersonBoxManagement.Tests.csproj --filter "FullyQualifiedName~BoxControllerTests"</automated>
  </verify>
  <done>Homepage table and lookup behavior satisfy BOX-08 and HOME-01 through HOME-04 with automated tests in the same plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> HomeController POST | Untrusted barcode text controls lookup and redirect. |
| Service -> dashboard view | Persisted box metadata is displayed to authenticated users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-S | Spoofing | Homepage routes | medium | mitigate | Preserve authenticated HomeController access and use claim-derived user display data. |
| T-02-03-T | Tampering | Barcode input | medium | mitigate | Trim and classify input server-side; only BOX values are eligible for box lookup. |
| T-02-03-I | Information Disclosure | Lookup errors | low | mitigate | Use generic not-found/warning messages without database internals. |
| T-02-03-R | Repudiation | Dashboard last user | low | mitigate | Display last user from persisted creator/modifier relationships without allowing client override. |
| T-02-03-SC | Tampering | NuGet installs | low | accept | No package installs in this plan. |
</threat_model>

<verification>
Focused command: `dotnet test MothersonBoxManagement.Tests/MothersonBoxManagement.Tests.csproj --filter "FullyQualifiedName~BoxControllerTests"`.
</verification>

<success_criteria>
BOX-08 and HOME-01 through HOME-04 are directly proven by focused tests and implemented in the homepage UI/controller/service flow.
</success_criteria>

<output>
Create `.planning/phases/02-box-lifecycle/02-03-SUMMARY.md` when done.
</output>
